spec:
  template:
    spec:
      initContainers:
      - name: fetch-keycloak-plugin
        image: ghcr.io/athenz-community/keycloak-token-exchange-identity-provider-manifest:latest
        imagePullPolicy: Always
        volumeMounts:
        - name: athenz-plugins
          mountPath: /opt/athenz/zts/lib/jars
      volumes:
      - name: athenz-plugins
        emptyDir: {}
